<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.longdatech.mybatisdo.dao.SysUser">

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM sys_user
    </select>

</mapper>